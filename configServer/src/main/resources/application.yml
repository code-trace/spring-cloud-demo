spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spring-cloud-demo/
          searchPaths: SpringcloudConfig
          username:
          password:
      label: master
server:
  port: 8888
  
